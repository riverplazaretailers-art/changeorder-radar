# ChangeOrder Radar — frontend configuration.
# No secrets belong in this file or in source. The browser only ever holds
# public configuration; the backend issues the session cookie.

# Base URL of the authoritative ChangeOrder Radar API.
# Leave unset/empty to run the clearly labelled demo adapter (synthetic data).
VITE_API_BASE_URL=
